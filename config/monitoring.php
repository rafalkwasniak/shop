<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Slow Query Threshold
    |--------------------------------------------------------------------------
    |
    | Database queries that run longer than this many milliseconds are written
    | to the dedicated "slow_queries" log channel together with their SQL,
    | bindings and the application origin (file:line). Set to 0 to disable
    | slow-query logging entirely.
    |
    */

    'slow_query_ms' => 200,

];
